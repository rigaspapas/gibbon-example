apiVersion: v1
kind: Service
metadata:
  name: {{ include "mini-api.fullname" . }}
  labels:
    {{- include "mini-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mini-api.selectorLabels" . | nindent 4 }}
