engine: helm

instructions:
  - name: "Apply the business values"
    arguments: template server -f business-values.yaml
  - name: "Apply the business and environment values"
    arguments: template server -f business-values.yaml -f environment-values.yaml
