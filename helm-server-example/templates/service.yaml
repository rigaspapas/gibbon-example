apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-server-example.fullname" . }}
  labels:
    {{- include "helm-server-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-server-example.selectorLabels" . | nindent 4 }}
